## 캐싱이란 ?
- 캐싱은 한 번 렌더링(또는 계산)한 결과를 저장해 두었다가, 같은 데이터를 다시 요청할 때 저장된 결과를 활용하는 기법
-  페이지를 다시 로드할 때 네트워크 요청이나 복잡한 연산을 줄일 수 있어 성능 개선에 도움을 준다

<br/>

## 캐싱의 필요성
### 성능 개선
자주 사용하는 데이터나 결과를 재사용하여 불필요한 연산/요청을 줄임

### 리소스 절약
서버의 부하를 감소시키고 네트워크 대역폭을 효율적으로 사용

### 사용자 경험 향상
빠른 응답 시간으로 페이지 로딩 시간을 단축시켜 UX 개선

<br/>

## 캐싱의 종류
### 클라이언트 측 캐싱
브라우저 캐시: HTML, CSS, JavaScript 등의 정적 자원을 브라우저에 저장
LocalStorage/SessionStorage: 자바스크립트를 이용해 데이터 저장 및 재사용

### 서버 측 캐싱
메모리 캐시: Redis, Memcached 등 인메모리 데이터 저장소를 사용
파일 시스템 캐시: 파일 단위로 결과를 저장하여 재사용

### CDN 캐싱
정적 자원을 전 세계 분산 서버에 저장해 사용자에게 가까운 서버에서 제공

<br/>

## 캐싱 전략
### Cache-Aside (Lazy Loading)
데이터를 요청할 때 캐시에 존재하지 않으면 DB나 원본에서 데이터를 가져와 캐시에 저장 후 반환

### Read-Through/Write-Through
캐시와 데이터베이스를 동기화하여 읽기/쓰기가 발생할 때 동시에 업데이트

### Time-To-Live (TTL)
캐시된 데이터에 만료시간을 설정해 일정 시간 후 자동으로 무효화

<br/>

## 캐싱 적용 시 고려사항
### 데이터 신선도
자주 업데이트 되는 데이터의 경우 캐시 무효화 전략(예: TTL, 이벤트 기반)을 고려

### 저장 위치
클라이언트, 서버, CDN 등 캐시 저장소의 특성을 고려하여 선택

### 캐시 크기 및 관리
캐시 오버플로우 및 메모리 사용량 관리

### 일관성 문제
분산 시스템에서 여러 캐시 인스턴스 간 데이터 동기화 문제 해결
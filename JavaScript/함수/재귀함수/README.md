재귀호출 인자에 증감연산자를 직접쓰면 의도랑 다르게 동작해서 무한루프가 잘터짐

### 재귀에서 문제 되는 이유
```js
function rec(a) {
  if (a === 0) return;
  console.log(a);
  rec(a--);   // 위험
}
```
a--는 현재 값 a를 인자로 전달하고, 그 후에야 a가 줄어듦

즉, 재귀에 들어갈 때는 여전히 같은 a 값 → 무한 재귀 발생

### 안전한 방법
인자로 넘길 때는 계산된 새 값을 명시
```js
rec(a - 1);   // 깔끔
```

혹은 전위 연산자 써도 되지만 가독성이 안 좋아서 추천 안 함
```js
rec(--a);     // 동작은 하지만 a가 바뀌면서 들어감
```

## 시드 (Seed)
쉽게 말하면 테스트나 앱을 돌리기 전에 DB에 미리 깔아두는 `예시 데이터`, 그리고 그걸 깔아두는 작업을 시드(seed)한다.

> 쇼핑몰 사이트에서 "상품 검색" 기능을 테스트하려는데, DB에 등록된 상품이 하나도 없으면 뭘 검색해도 검색결과가 없다.
> 그래서 테스트 전에 "테스트용 셔츠 1개"를 미리 등록해 둔다.
> 이 미리 깔아두는 행위가 시딩, 그 셔츠가 시드 데이터다.
## POMv(Page Object Model)
POM(Page Object Model)은 테스트 자동화를 위해 웹페이지를 객체로 추상화하는 디자인 패턴이다. 웹페이지의 요소(Locator)와 동작(Method)를 클래스로 분리해 캡슐화하는게 핵심

보통 페이지 클래스를 정의한 다음 테스트 스크립트에 적용한다.

코드를 작성할때 BasePage를 생성하여 이를 상속한 뒤 사용했는데 이는 페이지가 여러개일때는 상관없지만 한개라면 굳이.. ?